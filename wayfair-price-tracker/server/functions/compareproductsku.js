const db = require("../models/dbhelpers");

function compareProductSku(data) {
  console.log("we're here!!");
  console.log(JSON.stringify(data) + "this is data in compareproductsku");
}

module.exports = compareProductSku;
